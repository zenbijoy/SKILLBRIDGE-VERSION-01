{
  "name": "@skillbridge/edge-gateway",
  "version": "1.0.0",
  "description": "SkillBridge Next-Gen Cloudflare Worker Edge Gateway with SWR Caching and Origin Protection",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240329.0",
    "typescript": "^5.4.0",
    "wrangler": "^3.50.0"
  }
}
