# Antigravity Integration

Copy under `frontend/assets/nextgen-v2/` and extend the existing typed asset registry.

Suggested maps: `nextgenHeroBanners`, `nextgenFeatureCards`, `nextgenReactionStickers`, `nextgenAvatarFrames`, `nextgenBadges`, `nextgenNotifications`, `nextgenEmptyStates`, `nextgenGifs`.

Do not create a separate theme system. Keep v1 and v2 assets under the same SkillBridge design language.
